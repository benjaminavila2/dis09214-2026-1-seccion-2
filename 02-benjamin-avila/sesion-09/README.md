# sesión 09 - 12/06

# Crear un canvas con dimensiones dinámicas

# *PASO 1:*  crear canvas con dimensiones dinamicas (windowWidth, windowHeight)

# *PASO 2:* Function setup () { createCanvas(windowWidth, windowHeight) };
si el usuario estira o encoge la ventana del navegador, 
